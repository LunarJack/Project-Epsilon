from lib import lib;
func main()
{
    lib lib;
    var string stringVar = "hi";
    const double pi = 3.14159265;
    const double h = 6.62607015;
    const double store;
    if (pi = =3.14159265 && stringVar == "hi")
    {
        print(pi);
    }
    else 
    {
        print("Error! pi is not pi!");
    }
    print(stringVar);
    lib.add(pi, h, store);
    return pi;
}